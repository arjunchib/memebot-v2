mutation RenameMeme($name: String!, $newName: String!) {
  renameMeme(oldName: $name, newName: $newName) {
    _id
    name
    author {
      id
      name
    }
    url
    commands
    tags
    volume
    createdAt
  }
}
