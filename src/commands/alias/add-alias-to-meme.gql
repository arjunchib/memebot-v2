mutation AddAliasToMeme($name: String!, $alias: String!) {
  addAliasToMeme(name: $name, alias: $alias) {
    _id
    name
    author {
      id
      name
    }
    url
    commands
    tags
    volume
    createdAt
  }
}
